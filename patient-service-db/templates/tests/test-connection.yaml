apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "patient-service-db.fullname" . }}-test-connection"
  labels:
    {{- include "patient-service-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "patient-service-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
